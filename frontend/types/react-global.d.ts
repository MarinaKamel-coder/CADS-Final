/// <reference types="react" />

// Extend the global namespace to ensure React types are available
declare global {
  // React namespace is already provided by the reference above
  // This file ensures React types are in the global scope
}

export {};
