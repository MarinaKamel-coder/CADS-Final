{
"scripts": {
  "build": "npm install --prefix backend && npm install --prefix frontend && npx --prefix backend prisma generate && npm run build --prefix backend && npm run build --prefix frontend",
  "start": "npm start --prefix backend"
}
}